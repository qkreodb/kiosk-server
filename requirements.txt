# Kiosk Main Server (PORT 8080) — FastAPI backend
# Pinned versions for reproducible installs.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
edge-tts==7.2.8
# Piper: 오프라인(폐쇄망) TTS. `piper` 실행파일 제공 + ko_KR .onnx 음성 모델 필요.
# 1.4.x는 espeak-ng-data를 번들하고 onnxruntime만 의존 → 별도 시스템 설치 불필요.
piper-tts==1.4.2
playsound==1.2.2
python-dotenv==1.0.1
PyMySQL==1.1.1
# 라이브 CCTV: IP 카메라 RTSP 를 서버에서 디코딩(FFmpeg 내장) → MJPEG 중계.
# headless 휠은 GUI 의존성 없이 VideoCapture(RTSP) 를 지원한다.
opencv-python-headless==4.10.0.84
# ST80EL-USB warning-light control (imports as `hid`).
# Source builds may require: pkg-config, libusb-1.0-0-dev, libudev-dev.
hidapi==0.14.0.post4
