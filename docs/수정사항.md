# tts 관련 라이브러리 교체
- 현재 : Edge TTS (인터넷 필요, 텍스트를 Microsoft 서버로 보내야 함)
- 추후 : Piper TTS (최종 목표가 LLM에서 추출된 텍스트를 TTS로 재생)

# 현장 특이사항 관리가 있다면?
- 현재 : 더미 데이터
- 추후 : DB에 쌓인 VLM 분석 결과? 경고메시지? 출력

# 온습도 센서 상세 모달
- 현재 : 현재 온도, 상대 습도, 소음 레벨, 미세먼지 PM10을 알려줌.
- 추후 : 온습도 센서의 측정값 한계로 온도와 습도만 가능, UI적으로 수정(더미데이터로 대체 가능하지만 제외)

# 젯슨 IP, 데이터베이스 명, 비밀번호 일치성